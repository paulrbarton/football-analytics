name: Scrape Football Data and Upload to MotherDuck

on:
  # Run on schedule (daily at 6 AM UTC)
  schedule:
    - cron: '0 6 * * *'
  
  # Allow manual trigger
  workflow_dispatch:
    inputs:
      teams:
        description: 'Teams to scrape (comma-separated) or "all" for all teams'
        required: false
        default: 'Nottingham-Forest'
      season:
        description: 'Season (e.g., 2025-2026)'
        required: false
        default: '2025-2026'

jobs:
  scrape-and-upload:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r data_collection/requirements.txt
      
      - name: Create data directories
        run: |
          mkdir -p data/raw
          mkdir -p data/export
      
      - name: Run scraper (single team)
        if: ${{ github.event.inputs.teams != 'all' }}
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
          DATA_DESTINATION: motherduck
          DATABASE_NAME: football_analytics
          SCHEMA_NAME: raw
        run: |
          cd data_collection/scrapers
          python fbref_scraper.py
      
      - name: Run scraper (all teams)
        if: ${{ github.event.inputs.teams == 'all' }}
        env:
          MOTHERDUCK_TOKEN: ${{ secrets.MOTHERDUCK_TOKEN }}
          DATA_DESTINATION: motherduck
          DATABASE_NAME: football_analytics
          SCHEMA_NAME: raw
        run: |
          cd data_collection/scrapers
          python scrape_all_teams.py
      
      - name: Upload artifacts (backup)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scraped-data-${{ github.run_number }}
          path: |
            data/raw/*.csv
            data/export/*.parquet
          retention-days: 7
      
      - name: Report status
        if: always()
        run: |
          echo "Scraping completed"
          ls -lh data/raw/ || echo "No raw data files"
          ls -lh data/export/ || echo "No export files"
